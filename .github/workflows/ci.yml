name: CI

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: 10.x

    steps:
      - uses: actions/checkout@v2
      - name: build-and-test
      - run: |
          npm install
          npm test
          